# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

MIGRATION_TANSTACK_QUERY.md
styles.jsonPlan confirmado: Modularización de TemplatesSection
Estructura final
src/features/templates/components/
├── TemplatesSection.tsx      # Orchestration (~80 líneas)
├── TemplateHeader.tsx        # Header: badge + título + botones (era TemplatesSectionHeader)
├── TemplatesFilterBar.tsx   # Filtros: search + 3 selects
├── TemplatesTable.tsx        # Table wrapper + thead + empty + pagination
└── TemplateRow.tsx          # Fila individual + acciones